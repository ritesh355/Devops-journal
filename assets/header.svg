<?xml version="1.0" encoding="utf-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="300" viewBox="0 0 1200 300" preserveAspectRatio="xMidYMid slice">
  <defs>
    <!-- Background gradient: Deep Indigo to Electric Blue -->
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#1A1A40"/>
      <stop offset="100%" stop-color="#00BFFF"/>
    </linearGradient>

    <!-- Neon glow effect -->
    <filter id="neonBlur" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="6" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Background -->
  <rect width="1200" height="300" fill="url(#bg)"/>

  <!-- Main Text -->
  <g filter="url(#neonBlur)">
    <text x="50%" y="120" text-anchor="middle"
          font-family="Inter, Roboto, Arial, sans-serif"
          font-size="70" font-weight="700"
          fill="#FF5E78">
      Hi, I'm Ritesh Singh 🚀
    </text>
  </g>

  <!-- Bottom animated wave -->
  <path fill="#B0E0E680" transform="translate(0,160)">
    <animate attributeName="d" dur="6s" repeatCount="indefinite"
      values="
        M0,40 C300,100 900,-40 1200,40 L1200,0 L0,0 Z;
        M0,60 C300,80 900,0 1200,20 L1200,0 L0,0 Z;
        M0,40 C300,100 900,-40 1200,40 L1200,0 L0,0 Z" />
  </path>
</svg>
